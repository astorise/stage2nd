// Convertissez ces fonctions en fonctions fléchées

// Fonction 1: Calculer l'aire d'un rectangle
function calculerAire(longueur, largeur) {
  return longueur * largeur;
}

// Fonction 2: Vérifier si un nombre est pair
function estPair(nombre) {
  return nombre % 2 === 0;
}

// Fonction 3: Convertir en majuscules
function majuscules(texte) {
  return texte.toUpperCase();
}

// Convertissez ici :

