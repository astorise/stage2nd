// Créez des fonctions qui simulent des appels API :
// - obtenirUtilisateur(id) : retourne un utilisateur
// - obtenirCommandes(userId) : retourne les commandes d'un utilisateur
// - obtenirDetails(commandeId) : retourne les détails d'une commande

function obtenirUtilisateur(id) {
  // Votre code ici - utilisez Promise
}

function obtenirCommandes(userId) {
  // Votre code ici - utilisez Promise
}

function obtenirDetails(commandeId) {
  // Votre code ici - utilisez Promise
}

// Testez en chaînant les appels pour obtenir :
// 1. L'utilisateur avec l'ID 1
// 2. Ses commandes
// 3. Les détails de sa première commande
